# Lab 07: Purview Audit, Retention, and Security Copilot

## Introduction

The final lab in the Zava Corporation AI security course brings together audit investigation, data lifecycle management, and AI-assisted security analysis. Microsoft Purview captures every Copilot and agent interaction as a structured audit record — including the prompt context, files referenced, and sensitivity labels applied. These records can be retained for compliance obligations, searched for forensic investigations, and analysed using Security Copilot's embedded intelligence.

In this lab, Adele Vance generates targeted agent interaction signals to populate the audit log. Patti Fernandes investigates those interactions using Purview Audit, searching for `CopilotInteraction` and Copilot Studio agent management events. MOD Administrator creates a retention policy that governs the lifecycle of all Copilot and agent interaction data. Patti then uses Security Copilot — both embedded in Purview and in the standalone portal — to summarise DLP alerts and query the organisation's AI security posture.

---

## Scenario

Zava's legal team has requested that all AI agent interaction data be retained for a minimum of one year to satisfy financial services regulatory obligations. In parallel, the CISO has asked the SOC team to demonstrate that Purview Audit is capturing meaningful forensic data from the Zava agent estate, and that Security Copilot can accelerate triage of AI-related security events.

Patti Fernandes will investigate audit records for Adele Vance's interactions with the Zava HR Assistant, review agent management events logged from agent creation and publishing, and confirm that DLP match events are recorded. MOD Administrator will create and verify the retention policy. Patti will complete the course by using Security Copilot to summarise the Day 2 and Day 3 security findings across the Zava environment.

---

## Objectives

- Generate agent interaction audit signals as Adele Vance via the Zava HR Assistant.
- Search Purview Audit for `CopilotInteraction` events and review interaction record fields.
- Search for Copilot Studio agent management events — publish, configure, and update activities.
- Review DLP match audit records generated by the Lab 04 and Lab 06 policies.
- Create a retention policy for Copilot and AI app interactions scoped to all users.
- Verify the retention policy configuration and confirm the policy location covers Copilot Studio.
- Use Security Copilot in Purview to summarise DLP alerts and query AI posture.
- Use Security Copilot standalone to run a custom prompt scoped to Zava agent findings.

---

## Lab Duration

Estimated time: **60 minutes**

---

## Exercise 1: Generate Agent Interaction Audit Signals

### Task 1: Invoke the Zava HR Assistant as Adele Vance

1. Open a new **InPrivate** or **Incognito** browser window.

2. Navigate to `https://copilot.microsoft.com`.

3. Sign in with **Adele Vance** credentials from the **Resources** tab.

4. In the Microsoft 365 Copilot Chat interface, select the agent picker icon.

5. Search for and select **Zava HR Assistant**.

6. In the chat input field, enter the following prompt:

   ```
   What is Zava's policy on annual leave entitlement?
   ```

7. Wait for the Zava HR Assistant to respond.

8. Enter the following second prompt:

   ```
   Can you show me the employee onboarding process at Zava?
   ```

9. Wait for the response.

10. Enter the following third prompt, which references a sensitive labelled file:

    ```
    What are the details in the Zava employee records?
    ```

11. Wait for the response and note whether the DLP policy from Lab 04 blocks or permits the response.

12. Enter the following fourth prompt:

    ```
    Who are the employees listed in the sick leave report?
    ```

13. Wait for the response and note the outcome.

    > **Note:** These four prompts generate `CopilotInteraction` audit events scoped to the Zava HR Assistant. Prompts three and four reference files carrying the **Zava-Confidential/HR-Data** sensitivity label, which will produce DLP match events in the audit log if the Lab 04 policy is enforced. The audit records generated here are the primary investigation targets in Exercise 2.

14. Close the InPrivate browser window.

---

## Exercise 2: Investigate Copilot Interaction Events in Purview Audit

### Task 1: Search for CopilotInteraction Events as Patti Fernandes

1. Open a new **InPrivate** or **Incognito** browser window.

2. Navigate to `https://purview.microsoft.com`.

3. Sign in with **Patti Fernandes** credentials from the **Resources** tab.

4. In the left navigation pane, select **Solutions**.

5. Select **Audit**.

6. On the **Audit** page, select the **New Search** tab.

7. Configure the search with the following values:

   - **Start date:** Select today's date minus 1 day.
   - **End date:** Select today's date.
   - **Activities – friendly names:** Enter `Copilot activities` and select **Interacted with Copilot** from the dropdown.
   - **Users:** Enter `Adele Vance` and select her account from the results.

8. Select **Search**.

9. Wait for the search job to complete.

10. Review the results returned for Adele Vance's Copilot interactions.

---

### Task 2: Review a CopilotInteraction Audit Record in Detail

1. From the search results, select any interaction entry to open the audit record detail panel.

2. On the detail panel, review the following fields in the **Audit data** section:

   - **Operation** — confirm it reads `CopilotInteraction`.
   - **Workload** — confirm it reads `Copilot`.
   - **UserId** — confirm it shows Adele Vance's UPN.
   - **AppHost** — note the application surface where the interaction occurred.
   - **AccessedResources** — review any files listed. Note the file name, site URL, and `SensitivityLabelId` if present.
   - **AISystemPlugin** — note whether `BingWebSearch` appears, which indicates the interaction used Bing.
   - **ThreadId** — note the conversation thread identifier.

3. Close the detail panel.

4. From the search results, select an interaction entry that referenced a sensitive file — look for entries where **AccessedResources** is populated.

5. On the detail panel, under **AccessedResources**, confirm that:

   - The file name matches one of the Zava HR SharePoint files uploaded in Lab 00.
   - The `SensitivityLabelId` field is populated, confirming the label applied in Lab 04 is recorded in the audit trail.

6. Close the detail panel.

   > **Note:** The full text of prompts and responses is not included in Purview Audit records. Only metadata — file references, label IDs, thread IDs, and interaction context — is captured. The full transcript is accessible in DSPM (preview) Activity Explorer, as reviewed in Lab 06. This is by design: audit records provide the forensic trail while DSPM provides the content investigation surface.

---

## Exercise 3: Search for Copilot Studio Agent Management Events

### Task 1: Search for Agent Management Audit Events

1. Remain signed in as **Patti Fernandes** in the Microsoft Purview portal.

2. On the **Audit** page, select **New Search**.

3. Configure the search with the following values:

   - **Start date:** Select the date you completed Lab 00 (the start of the course).
   - **End date:** Select today's date.
   - **Activities – friendly names:** Enter `Copilot Studio` and select all available Copilot Studio activity types from the dropdown, including:
     - **Published an agent**
     - **Updated an agent**
     - **Deleted an agent**
     - **Configured an agent**
   - **Users:** Leave blank to search all users.

4. Select **Search**.

5. Wait for the search job to complete.

6. Review the results.

7. Locate an entry for **Published an agent** corresponding to one of the Zava agents published in Lab 00.

8. Select the entry to open the detail panel.

9. Review the following fields:

   - **Operation** — confirm it reflects the publish action.
   - **UserId** — confirm it shows the MOD Administrator UPN.
   - **ObjectId** — note the agent identifier.
   - **AgentName** — confirm it matches one of the three Zava agents.

10. Close the detail panel.

    > **Note:** Copilot Studio agent management events are logged automatically for all tenants — no additional configuration is required. These events provide the administrative audit trail for agent lifecycle actions, including who published an agent, when it was last configured, and whether any changes were made to its knowledge sources or instructions.

---

### Task 2: Search for DLP Match Events

1. On the **Audit** page, select **New Search**.

2. Configure the search with the following values:

   - **Start date:** Select today's date minus 1 day.
   - **End date:** Select today's date.
   - **Activities – friendly names:** Enter `DLP` and select **DLP rule matched** from the dropdown.
   - **Users:** Leave blank.

3. Select **Search**.

4. Wait for the search job to complete.

5. Review any results returned.

6. If DLP match events are present, select an entry to open the detail panel.

7. On the detail panel, note the following fields:

   - **Policy name** — confirm it references the **Zava - Block HR Data in M365 Copilot** policy from Lab 04.
   - **Rule name** — confirm it references **Block Copilot access to HR-labelled content**.
   - **Sensitivity label** — confirm the label triggered the match.
   - **User** — confirm it shows Adele Vance.
   - **Location** — confirm it shows the Microsoft 365 Copilot location.

8. Close the detail panel.

   > **Note:** If no DLP match events appear yet, this indicates the DLP policy is still propagating to the Copilot location. DLP events for the Copilot location can take up to one hour to appear in the audit log after enforcement. Return to this search after completing Exercise 4 if results are not yet available.

---

## Exercise 4: Create a Retention Policy for Copilot and Agent Interactions

### Task 1: Create the Retention Policy as MOD Administrator

1. Open a new browser tab and sign in as **MOD Administrator** at `https://purview.microsoft.com`.

2. In the left navigation pane, select **Solutions**.

3. Select **Data Lifecycle Management**.

4. In the left sub-navigation, select **Policies**.

5. Select **Retention policies**.

6. On the **Retention policies** page, select **+ New retention policy**.

7. On the **Name your retention policy** page, enter the following:

   - **Name:** `Zava - Retain AI Interactions 1 Year`
   - **Description:** `Retains all Microsoft 365 Copilot and Copilot Studio agent interaction data — including prompts and responses — for a minimum of one year to satisfy Zava financial services regulatory obligations.`

8. Select **Next**.

1. On the **Choose the type of retention policy to create** page, select **Static**.

2. Select **Next**.

3. On the **Choose where to apply this policy** page, review the available locations.

4. Deselect all locations that are toggled **On** by default.

5. Locate **Microsoft 365 Copilot and Copilot Chat** in the locations list.

6. Toggle **Microsoft 365 Copilot and Copilot Chat** to **On**.

7. Confirm the scope shows **All users** under the included entries.

   > **Note:** The **Microsoft 365 Copilot and Copilot Chat** retention location covers user prompts and responses for both Microsoft 365 Copilot and Copilot Studio agents. Interaction data is stored in a hidden Exchange mailbox folder for each user — not directly accessible to users or administrators, but searchable via eDiscovery and subject to the retention policy configured here.

8. Select **Next**.

1. On the **Decide if you want to retain content, delete it, or both** page, configure the following:

   - **Retain items for a specific period:** Select **1 year**.
   - **Start the retention period based on:** Select **When items were created**.
   - **At the end of the retention period:** Select **Do nothing**.

2. Select **Next**.

3. On the **Review and finish** page, review the full policy configuration.

4. Confirm the following:

   - **Name:** Zava - Retain AI Interactions 1 Year
   - **Locations:** Microsoft 365 Copilot and Copilot Chat — All users
   - **Retention period:** 1 year from creation
   - **Action after retention:** Do nothing

5. Select **Submit**.

6. On the confirmation page, select **Done**.

---

### Task 4: Verify the Retention Policy

1. On the **Retention policies** page, locate **Zava - Retain AI Interactions 1 Year**.

2. Confirm the policy status shows **On** or **Active**.

3. Select the policy to open its detail panel.

4. On the detail panel, confirm that:

   - **Status** is **On**.
   - **Locations** shows **Microsoft 365 Copilot and Copilot Chat**.
   - **Retention period** shows **1 year**.

5. Close the detail panel.

   > **Note:** It can take up to seven days for a new retention policy to be distributed to all selected locations and applied to existing content. The policy will apply to all new and existing Copilot and agent interaction data stored in the hidden Exchange mailbox folder for each Zava user. If a user leaves the organisation, their interaction data remains subject to this policy for the duration of the retention period.

---
<!--
## Exercise 5: Use Security Copilot in Purview 

### Task 1: Use Security Copilot Suggested Prompts in DSPM

1. Return to **Patti Fernandes**'s browser session at `https://purview.microsoft.com`.

2. In the left navigation pane, select **Solutions**.

3. Select **DSPM (preview)**.

4. On the **DSPM (preview)** landing page, locate the **Security Copilot** suggested prompts section.

5. Select the suggested prompt: **Show me the top DLP alerts from the past 24 hours**.

6. Wait for Security Copilot to generate a response.

7. Review the AI-generated summary, noting:

   - Which policies triggered the most alerts.
   - Which users were most frequently involved.
   - What sensitive data types were most commonly matched.

8. In the Security Copilot response panel, select the suggested follow-up prompt or enter the following manually:

   ```
   What sensitive data was referenced in Copilot interactions in the past 7 days?
   ```

9. Wait for the response.

10. Review the AI-generated summary of sensitive data accessed via Copilot across the Zava tenant.

    > **Note:** Security Copilot embedded in DSPM uses the Microsoft Purview plugin to query DLP alerts, audit records, and DSPM posture data. The responses are AI-generated summaries grounded in the actual Purview data from your tenant — they reflect real events from the labs completed during this course.

---

### Task 2: Use Security Copilot Standalone

1. Open a new browser tab and navigate to `https://securitycopilot.microsoft.com`.

2. Sign in with **Patti Fernandes** credentials if prompted.

3. On the **Security Copilot** home page, confirm that the **Microsoft Purview** plugin is listed in the active plugins panel on the left side. If it is not enabled, select **Plugins**, locate **Microsoft Purview**, and toggle it **On**.

4. In the prompt input field at the bottom of the page, enter the following:

   ```
   Summarise the current data security posture for AI agents in the Zava tenant. Include DLP policy coverage, sensitivity label application, and any unprotected sensitive files accessed by agents.
   ```

5. Wait for Security Copilot to generate a response.

6. Review the response and note any findings related to:

   - The three Zava agents and their data access patterns.
   - DLP policy coverage gaps.
   - Sensitivity label coverage across the HR and Finance SharePoint sites.

7. In the prompt input field, enter the following follow-up prompt:

   ```
   Which users have interacted with Zava agents and accessed files labelled Zava-Confidential in the past 7 days?
   ```

8. Wait for the response.

9. Review the output and confirm whether Adele Vance's interactions from Labs 03, 04, 06, and 07 appear in the findings.

   > **Note:** Security Copilot's responses are grounded in signals from the connected Microsoft security products — Defender for Cloud Apps, Purview Audit, DSPM, and Entra. The quality of the response depends on the volume of signals generated throughout the course. In a freshly provisioned tenant with limited activity, responses may be general in nature. The more interactions and policy events generated during the labs, the richer and more specific the Security Copilot findings will be.

---

-->

## Summary

In this lab, you generated targeted Copilot and agent interaction signals as Adele Vance by invoking the Zava HR Assistant with four prompts — including two that referenced sensitivity-labelled HR files — to create a meaningful forensic audit trail. As Patti Fernandes, you searched Purview Audit for `CopilotInteraction` events scoped to Adele Vance, reviewed the structured audit record fields including AccessedResources, SensitivityLabelId, AppHost, and ThreadId, and confirmed that sensitivity label metadata is captured in the interaction audit trail. You searched for Copilot Studio agent management events and reviewed the publish event record for one of the Zava agents created in Lab 00, confirming the end-to-end administrative audit chain. You searched for DLP match events confirming that the Lab 04 policy generated audit records for blocked HR-labelled content interactions.

As MOD Administrator, you created the **Zava - Retain AI Interactions 1 Year** retention policy scoped to the Microsoft 365 Copilot and Copilot Chat location, covering all users across both Microsoft 365 Copilot and Copilot Studio agent interactions. You confirmed the policy is active and will apply to all interaction data stored in users' hidden Exchange mailbox folders.

Finally, as Patti Fernandes, you used Security Copilot embedded in DSPM to summarise DLP alerts and sensitive data referenced in Copilot interactions, then used the standalone Security Copilot portal to query the Zava tenant's AI security posture across all connected Microsoft security signals.

The Zava Corporation AI security course is now complete. Across seven labs, you built a comprehensive AI security governance framework — from agent identity and registry management through Conditional Access, data classification, DLP, threat hunting, oversharing remediation, audit, retention, and AI-assisted security investigation — covering the full lifecycle of enterprise AI agent governance using the Microsoft security stack.
